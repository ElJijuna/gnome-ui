[project]
name = "gnome-ui-docs"
version = "0.1.0"
description = "Doc tooling for the gnome-ui documentation site (MkDocs + Material)"
requires-python = ">=3.11"
dependencies = [
    "mkdocs-material>=9.6",
    "mkdocs-git-revision-date-localized-plugin>=1.3",
]

[tool.uv]
package = false
