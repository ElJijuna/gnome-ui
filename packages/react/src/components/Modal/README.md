# Modal

Large overlay for rich content, forms, and settings. Unlike `Dialog` (which targets confirmations and alerts), `Modal` provides a scrollable body and an integrated header bar with a close button plus optional primary and secondary actions.

Use `Dialog` for short confirmations. Use `Modal` for content-heavy flows.
